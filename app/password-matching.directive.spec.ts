import { PasswordMatchingDirective } from './password-matching.directive';

describe('PasswordMatchingDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordMatchingDirective();
    expect(directive).toBeTruthy();
  });
});
